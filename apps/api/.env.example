# Supabase Postgres connection string.
# Use the "Connection pooling" URL for the running app, and the direct
# connection string for `prisma migrate` if pooling gives you trouble.
DATABASE_URL="postgresql://postgres:password@localhost:5432/civic_platform"

PORT=4000
